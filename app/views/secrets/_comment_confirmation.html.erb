<div class="modal-header text-dark">
  <h5 class="modal-title">Delete Post</h5>
</div>
<div class="modal-body">
  <div class="container text-dark">
    <p class="lead">
      Are you sure you want to delete this comment? 
    </p>
  </div>
</div>
<div class="modal-footer">
  <button type="button" 
  class="btn btn-primary fw-bold text-light"
  data-bs-dismiss="modal"
  aria-label="Cancel">Cancel</button>

  <% if comment.commentable_type == "Secret"%>
  <% linked_path = secret_comment_url(id: comment.id, secret_id: comment.commentable_id)%>
  <% else %>
  <% linked_path = comment_comment_url(id: comment.id, comment_id: comment.commentable_id)%>
  <% end %>
  <%= link_to "Delete", linked_path, 
  method: :delete,
  remote: true,
  class: "btn btn-danger",
  "data-bs-dismiss" => "modal" %>
</div>